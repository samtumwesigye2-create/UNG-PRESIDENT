name: Presidential Security CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [security/presidential-hardening]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm test -- __tests__/security-headers.test.ts
